# Build output
/build/
/build-*/

# User-supplied copyrighted content (see README.md)
/Disk.1
/Disk.2
/Disk.3
/harness/

# Game memory dumps / extracted assets (regenerable from your own disks)
chip_ram_dump.bin
chip_ram_dump.*.bin
assets.bin
/assets/
game_binary.bin
game_data.bin

# Recompiled 68k->C code: a derivative of the copyrighted game, generated from
# the user's own Disk.* images at build time (tools/regen.sh, auto-run by CMake).
# NOT shipped — see docs/build-and-run.md.
/src/engine/generated/

# Runtime logs / framebuffer dumps / traces
/logs/
frame_dump.bmp
blit_trace_pc.txt
*.gdb

# Editor / OS / Python
__pycache__
*.pyc
.DS_Store
.claude/
.vscode/
scratch/

# User config (copy from benefactor.example.json)
/benefactor.json
profile.json
/Disk.4
